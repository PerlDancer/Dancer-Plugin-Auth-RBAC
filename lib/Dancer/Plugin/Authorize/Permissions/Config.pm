# ABSTRACT: Access control via the Dancer configuration file!

package Dancer::Plugin::Authorize::Permissions::Config;

use strict;
use warnings;

sub subject_asa {
    
    my ($class, $options, @arguments) = @_;
    my $settings = $class::settings;
    
}

sub subject_can {
    
    my ($class, $options, @arguments) = @_;
    my $settings = $class::settings;
    
}

1;